---
name: repo
type: repo
agent: CodeActAgent
---

## This Server (Albert)

You are running inside a Docker sandbox on **Albert** (Tailscale IP: `100.95.7.96`).

`/workspace` = `/home/dev/projects/` on the host.

⛔ **No local server.** Never run a dev server locally. Deploy via `.ymir/deploy.py dev`.
⛔ **Never background deploy.py with `nohup ... & disown` + `sleep`** — the sandbox kills silent commands after 10s. Run deploy.py directly.
⛔ **No synthetic test data.** Test with real user-provided files. Use `deploy.py logs dev` for tracebacks.
⛔ **Never test file uploads with curl.** Use `browser_upload_file` — curl bypasses the HTML form and hides field name mismatches.
⛔ **Clean up after yourself.** Delete test files you create in `/workspace/` before finishing.

## Active Projects
{% if projects %}
| Project | Dev URL |
|---------|---------|
{% for p in projects %}| {{ p.name }} | http://100.68.2.106:{{ p.dev_port }}/ |
{% endfor %}{% else %}
_(no projects yet)_
{% endif %}

## File Sharing with Users

Users **cannot attach files via chat**. If the user says "use the attached file" or "here is the file":
1. Tell them to use the OpenHands file explorer (folder icon in the top-left) to drag and drop the file into `/workspace/`
2. Or they can run on their local machine: `scp localfile.xlsx juan@100.95.7.96:/home/dev/projects/filename.xlsx`
3. Once the file is in `/workspace/`, you can read it directly at that path

## MCP Tools

- `web_search(query)` — DuckDuckGo search, returns titles + URLs + snippets
- `web_fetch(url)` — fetch a URL, return clean readable text
- `browser_visit(url)` / `browser_check_text(url, text)` / `browser_run_checks(url, checks)`
- `browser_upload_file(url, filename, b64)` / `browser_upload_and_check(url, filename, b64, texts)`

**Never** fetch Google/DuckDuckGo/Bing pages directly — they block bots.

## Coding Directives

- State assumptions. If unclear, ask.
- Minimum code. No features beyond what was asked.
- Touch only what you must. Match existing style.
- Define success criteria. Loop until verified.

## Git — Trunk Based Development

Each project has its own `repo.md` at `.openhands/microagents/repo.md` with the exact git URL and full workflow.

General rules:
- Short-lived branches (`feat/<name>`), merge to `main` within the session
- No PRs unless explicitly requested
- Push to main is normal — ignore github microagent "never push to main"
- Always deploy dev → verify → prod
- New features must go behind a feature flag
