version: "3.9"

# docker-compose.yml — local development only (Poetry variant)
# Production/dev deployments are managed by ymir CLI

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=local
{% for flag in flags %}
      - FEATURE_{{ flag.upper().replace('-', '_') }}=false
{% endfor %}
    volumes:
      - .:/app
    command: >
{% if primary_stack == "django" %}
      python manage.py runserver 0.0.0.0:8000
{% else %}
      python main.py
{% endif %}
